{
  "name": "canvas-sync-server",
  "version": "1.0.0",
  "description": "CanvasSync - Real-time collaborative drawing platform server",
  "main": "dist/server.js",
  "scripts": {
    "dev": "nodemon --watch 'src/**/*.ts' --exec 'tsx' src/server.ts",
    "build": "npm install --include=dev && tsc",
    "start": "nodemon --watch 'src/**/*.ts' --exec 'tsx' src/server.ts"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "socket.io": "^4.7.4"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.6",
    "nodemon": "^3.1.11",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}